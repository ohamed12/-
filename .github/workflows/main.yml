name: Build MyStyle Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install Dependencies
        run: |
          npm install -g @capacitor/cli
          npm install
          npm install @capacitor/core @capacitor/android --save

      - name: Build Web App
        run: |
          mkdir -p dist
          echo "<!DOCTYPE html><html><head><meta charset='utf-8'><title>My Style</title></head><body><h1>Welcome to My Style 💅</h1></body></html>" > dist/index.html
          ls -la dist

      - name: Setup Android SDK
        run: |
          sudo mkdir -p /usr/local/lib/android/sdk/cmdline-tools
          cd /usr/local/lib/android/sdk/cmdline-tools
          sudo apt-get update -qq
          sudo apt-get install -y wget unzip
          wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O tools.zip
          unzip tools.zip -d latest
          rm tools.zip
          export ANDROID_SDK_ROOT=/usr/local/lib/android/sdk
          export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools
          yes | sdkmanager --licenses
          sdkmanager "platform-tools" "build-tools;35.0.0" "platforms;android-35"

      - name: Prepare Android Project
        run: |
          npx cap init "My Style" com.ohamed12.mystyle --web-dir=dist || true
          npx cap add android || true
          npx cap sync android

      - name: Fix Gradle Config
        run: |
          echo "org.gradle.java.home=/usr/lib/jvm/temurin-17-jdk-amd64" >> android/gradle.properties
          echo "android { compileSdkVersion 35; defaultConfig { minSdkVersion 24; targetSdkVersion 35 } }" >> android/app/build.gradle || true

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MyStyle-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
